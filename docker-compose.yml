version: "3.8"

services:
  api:
    build: .
    container_name: lehu-update-feedback
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
